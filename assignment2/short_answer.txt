Before submitting this file, make sure that there are no more TODO
placeholders remaining in the file (and remove this comment too).

Marriage Pact
-------------

Q1. It is your choice to use either an ordered or unordered set. In a few sentences, what are some of the tradeoffs between the two? Additionally, please give an example (that has not been shown in lecture) of a valid hash function that could be used to hash student names for an unordered set.
A1. Ordered sets use a red-black tree to store elements which preserve order at the cost of O(logn) time complexity operations. Unordered sets use hash functions to store elements in an unordered way but with O(1) time complexity operations. A valid hash function has to output a non-negative integer for a key. One example is DJB2 which is a hash function for strings.

Q2. Note that we are saving pointers to names in the queue, not names themselves. Why might this be desired in this problem? What happens if the original set where the names are stored goes out of scope and the pointers are referenced?
A2. Saves memory. If original set is not on the stack anymore then pointers will reference non-existent memory which can lead to undefined behavior.